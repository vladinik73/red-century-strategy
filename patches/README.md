# LEGACY / NON-CANONICAL — kept for history; do not use for implementation

This directory contains historical patch fragments and snapshots that were applied to canonical docs and schemas during prior version cycles (v4.2–v4.22). Canonical sources are:

- **Schemas:** `schemas/*.json` (match, tile, player, city, unit)
- **Docs:** `docs/` (see `docs/00_meta/REPO_STRUCTURE.md`)

Do not reference patches/ for current development. For spec lock and version matrix, see `docs/00_meta/SOURCE_OF_TRUTH.md` and `docs/00_meta/CHANGELOG.md`.
